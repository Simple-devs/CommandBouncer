############################################################
# +------------------------------------------------------+ #
# |                       Notes                          | #
# +------------------------------------------------------+ #
############################################################
# This plugin was created by: 
# Carlgo11 http://www.carlgo11.com/
# Psgs http://www.psgs.tk/

# Get in touch with us:
# Join our irc: http://bit.ly/cmdbouncerirc

# DO NOT EDIT THE CONFIG-VERSION!
config-version: 1.0
############################################################
# +------------------------------------------------------+ #
# |                  General Settings                    | #
# +------------------------------------------------------+ #
############################################################

# Does this plugin not work? Set "debug" to true and pastie your server.log to a developer
# true/false
debug: false

# Want to help us improve this plugin? Send us anonymus statics about our plugin. (Recommended: 'true')
opt-out: true

# Do you want the plugin to update automatically when a new version is available? (Recommended: 'true')
auto-update: true

# If auto-update is set to false, who should receive information when a new update is available? (Recommended: 'perm')
# Available options: op, perm, none
warn-update: perm

# Would you like to to backup the current config and replace it with a new one when a new version of CommandBouncer is downloaded? (Might cause you some extra work when a new version is downloaded)
update-config: false

# When seeking assistance via /CommandBouncer support/report should the plugin upload your latest.log? (Recommended: 'true')
send-log: true


############################################################
# +------------------------------------------------------+ #
# |                       Settings                       | #
# +------------------------------------------------------+ #
############################################################

# Enter the worlds you want to ignore players in.
disabled-worlds:
- a_disabled_world
- another_disabled_world

# Enter the players you want to ignore. (You can also remove the permissions from the players)
ignored-players:
- anminecrafter
- zyxep

# Do you want to disable the incoming message when a command matches a command listed below?
# Example: player types /op & the command is set to be disabled /op won't be executed but the bounce-command will. 
# If a cmd is not set below, it will not be disabled.
disabled-commands:
- cmd1

# This setting enables the plugin to search for commands that only starts with the command. 
# For example if a cmd<number> is set to ban it will react on /ban admin and /ban player. (admin and player being an actuall user)
startswith-commands:
- cmd1
############################################################
# +------------------------------------------------------+ #
# |                   Listen Commands                    | #
# +------------------------------------------------------+ #
############################################################

# Enter the commands you want the plugin to listen on. Use the same number on the listening command as on the bouncer command!
cmd1: cmdbnctest

############################################################
# +------------------------------------------------------+ #
# |                   Bounce Commands                    | #
# +------------------------------------------------------+ #
############################################################

# Enter the commands that you want the plugin to execute when a player types the given command
console1:
- tell %player% hello %player%!

